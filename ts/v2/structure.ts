import * as THREE from "three";
import { Construction } from "./construction";
import { Composition } from "./composition";

// A man-made object
export class Structure extends Composition implements Construction {
    // Loads and saves from JSON.

}